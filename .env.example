# ========================================
# DATABASE (Supabase)
# ========================================
# Supabase Pooler (Transaction mode) - for serverless runtime
# Get from: Supabase Dashboard → Settings → Database → Connection Pooling → Transaction
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres"

# Session pooler (for migrations only) - supports prepared statements
# Get from: Supabase Dashboard → Settings → Database → Connection Pooling → Session
DIRECT_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres"

# ========================================
# AUTHENTICATION (NextAuth.js v5)
# ========================================
# Production URL (no trailing slash)
NEXTAUTH_URL="https://your-domain.com"

# Generate with: openssl rand -base64 32
AUTH_SECRET="your-secret-key-here"

# Google OAuth - https://console.cloud.google.com/apis/credentials
AUTH_GOOGLE_ID="your-google-client-id"
AUTH_GOOGLE_SECRET="your-google-client-secret"

# ========================================
# STRIPE (Payments)
# ========================================
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY="sk_live_..."
STRIPE_PUBLISHABLE_KEY="pk_live_..."

# Get from: Stripe Dashboard → Developers → Webhooks → Signing secret
STRIPE_WEBHOOK_SECRET="whsec_..."

# Create products in Stripe and get Price IDs
STRIPE_STARTER_PRICE_ID="price_..."
STRIPE_PROFESSIONAL_PRICE_ID="price_..."
STRIPE_ENTERPRISE_PRICE_ID="price_..."

# ========================================
# UPSTASH REDIS (Rate Limiting + Caching)
# ========================================
# Get from: https://console.upstash.com → Redis → REST API
UPSTASH_REDIS_REST_URL="https://xxx.upstash.io"
UPSTASH_REDIS_REST_TOKEN="AXxx..."

# ========================================
# UPSTASH QSTASH (Background Jobs / Email Queue)
# ========================================
# Get from: https://console.upstash.com → QStash → Details
QSTASH_TOKEN="eyJxx..."

# Get from: QStash → Signing Keys
QSTASH_CURRENT_SIGNING_KEY="sig_xxx..."
QSTASH_NEXT_SIGNING_KEY="sig_xxx..."

# ========================================
# EMAIL (Resend)
# ========================================
# Get from: https://resend.com/api-keys
RESEND_API_KEY="re_..."
EMAIL_FROM="KinderCare <noreply@yourdomain.com>"

# ========================================
# FILE UPLOADS (Uploadthing)
# ========================================
# Get from: https://uploadthing.com/dashboard
UPLOADTHING_SECRET="sk_live_..."
UPLOADTHING_APP_ID="..."

# ========================================
# MAPS (Mapbox) - Optional
# ========================================
# Get from: https://account.mapbox.com/access-tokens/
NEXT_PUBLIC_MAPBOX_TOKEN="pk...."

# ========================================
# ERROR TRACKING (Sentry)
# ========================================
# Get from: https://sentry.io → Project Settings → Client Keys (DSN)
# Server-side DSN
SENTRY_DSN="https://xxx@xxx.ingest.sentry.io/xxx"
# Client-side DSN (same as SENTRY_DSN)
NEXT_PUBLIC_SENTRY_DSN="https://xxx@xxx.ingest.sentry.io/xxx"

# Get from: Sentry → Settings → Auth Tokens (for source map uploads)
SENTRY_AUTH_TOKEN="sntrys_..."
SENTRY_ORG="your-org"
SENTRY_PROJECT="kindergarten"

# ========================================
# APP SETTINGS
# ========================================
NEXT_PUBLIC_APP_URL="https://your-domain.com"
NODE_ENV="production"
